Starwake Protocol verified rebuild

Root cause fixed:
- game.js referenced savedAudioEnabled and savedAudioVolume before those constants were initialized.
- This caused a JavaScript temporal-dead-zone ReferenceError and stopped all startup code.

Validation performed:
- node --check game.js
- node --check musicEngine.js
- Browser startup test in Chromium
- Continue button test
- Launch Protocol button test
- Confirmed start menu closes and game canvas initializes
- Confirmed localStorage restrictions do not prevent startup

Deployment:
Upload index.html, styles.css, game.js, and musicEngine.js together at the same directory level.
